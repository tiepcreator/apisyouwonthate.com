---
pageable: true
per_page: 10
layout: layouts/articles
---

%section.section.section--large
  .container
    %h2.text-center.padding-bottom--40 Articles

    %ul.articles
      - page_articles.each_with_index do |article, i|
        %h2
          = link_to article.title, article 
          %span= article.date.strftime('%b %e')

        = article.body
